<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileTracking.Pages.LoginPage"
             xmlns:handlers="clr-namespace:MobileTracking.Handlers"
             Title="LoginPage">
    <Grid VerticalOptions="FillAndExpand" >


        <VerticalStackLayout VerticalOptions="Center">
        <Border>
                <handlers:BorderlessEntry Placeholder="Entrar"></handlers:BorderlessEntry>
        </Border>
        <Border>
            <Label Text="Não tenho acesso."></Label>
        </Border>
        </VerticalStackLayout>
        <VerticalStackLayout VerticalOptions="End">
            <Border>
                <Label Text="Entrar"></Label>
            </Border>
        </VerticalStackLayout>
    </Grid>
</ContentPage>